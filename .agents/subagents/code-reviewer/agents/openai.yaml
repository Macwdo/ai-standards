interface:
  display_name: "Code Reviewer"
  short_description: "Review code for correctness, risk, and maintainability"
  default_prompt: "Use the code-reviewer subagent to review these changes and return prioritized findings."
